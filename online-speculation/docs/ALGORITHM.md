# 分块起草、验证解码与在线蒸馏：从概率基础到完整管线

本文随实现和实验结果持续更新。
命令见 [RUNNING.md](RUNNING.md)，实现目录见 [README](../README.md)。

**来源与归属。** 条件低秩适配、块去噪蒸馏和两遍验证解码的研究起点是
[Uno：Unlocking Lossless Speedups in LLMs via Discrete Diffusion](https://arxiv.org/abs/2609.04010)。
本项目据此编写模型、训练与推理实现，推导概率关系，并用可枚举问题、数值微分和外部模型对照验证。

主线是：**已有自回归基座 → 从零初始化草稿适配器 → 离线蒸馏 → 验证解码 → 推理中的适配器续训**。
基座采用预训练权重；适配器从随机初始化开始离线学习，随后在推理请求中继续训练。

## 1. 我们究竟要改变什么

token 是词表中的一个编号，可能表示一个字、词的一部分或标点。语言模型看到的是整数序列。
给定已经确定的文本 h，模型给出“下一个 token 是什么”的概率。
设词表为 $\mathcal V=\{0,\ldots,V-1\}$，基座概率记为 $p_\theta(v\mid h)$：

- $\theta$ 是始终冻结的基座权重。
- p 是最终输出应服从的目标概率表。
- $q_\phi$ 是草稿概率表，由可训练参数 $\phi$ 控制，用来近似目标 p。

例如词表为 A、B、C，$p=(0.2,0.5,0.3)$ 表示反复抽样时，长期频率约为 20%、50%、30%。
单次抽样按这张表随机选择一个 token。

模型先输出任意实数 $l_v$，称为 logits，再变成概率：

```math
p_v=\frac{\exp(l_v)}{\sum_{u\in\mathcal V}\exp(l_u)}.                  \tag{1}
```

这叫 softmax，所有概率经过分母归一化后和为 1。温度 $\tau>0$ 时用 $l_v/\tau$；温度为零另行定义为贪心：
选最大 logit，概率表变成一个 1 和其余 0。top-k 保留最大的 k 项；top-p 保留按概率降序累计达到
阈值的最短前缀，再归一化。实现保留使累计概率跨过阈值的那一项。

候选抽样与概率校正使用相同的温度和截断设置。
[sampling.py](../src/blockspec/sampling.py) 在抽样之前构造这张概率表。
贪心是一个可以化简的特例：最大项的概率为 1，其余项为 0，最终输出等于老师的最大项。
因此可以用 argmax 直接实现这个特例。
当前 AR、线性和树解码都直接计算 argmax；小词表穷举检验了化简前后逐步结果一致。
相等的最大值按同一个 argmax 顺序打破平局。

## 2. 自回归为什么串行，验证为什么可以并行

自回归（AR）是逐项生成，每一步依赖之前的输出。乘法法则给出：

```math
p_\theta(y_1,\ldots,y_n\mid h)
=\prod_{i=1}^n p_\theta(y_i\mid h,y_1,\ldots,y_{i-1}).                \tag{2}
```

第二项的条件包含第一项，所以正常生成先得到 $y_1$，再确定第二步输入。
给定候选 A、B、C，可以把它们一次送进模型，让每个位置按因果规则读取自己的前缀。
一遍前向就得到这条候选路径上所有位置的“下一项概率”。

这个读取规则叫因果注意力：每个位置读取自身及其之前的输入。
因此“先起草后续多项，再一起验证”有机会减少串行前向次数。

**输入位置 j 的 logits 预测 j+1 的 token。**
干净分支、带噪分支和验证分支都保持这一约定。

### 2.1 模型中的矩阵在做什么

嵌入表把 token 编号变成向量 x，线性层是 $Wx$。注意力产生查询、键、值三类向量：

```math
Q=XW_Q^\top,\quad K=XW_K^\top,\quad V=XW_V^\top,
\qquad H=\operatorname{softmax}\left(\frac{QK^\top}{\sqrt d}+M\right)V. \tag{3}
```

这里 $\top$ 表示矩阵转置。可读位置在 M 中取 0，屏蔽位置取负无穷，softmax 后屏蔽位置的权重为零。
多头注意力并行使用多组这样的向量；GQA 让多个查询头共享键和值，减少缓存。

RMSNorm 按均方根缩放向量，再乘权重：

```math
\operatorname{RMSNorm}(x)_i
=w_ix_i\left(\frac1d\sum_j x_j^2+\epsilon\right)^{-1/2}.              \tag{4}
```

前馈层为 $W_{down}[\operatorname{SiLU}(W_{gate}x)\odot(W_{up}x)]$，
$\odot$ 是逐项相乘，$\operatorname{SiLU}(a)=a/(1+e^{-a})$；残差连接把子层输出加回输入。
这些运算组成了本项目的 Transformer。

位置编码对成对分量做旋转，角度为“位置 × 频率”，基础频率 $\omega_j=\vartheta^{-2j/d}$。
当前本地基座使用 YaRN：按维度平滑混合原频率和除以扩展倍数后的频率，并应用配置中的幅度因子。
频率保持 FP32，RMSNorm 的舍入位置按基座架构设置。
[model.py](../src/blockspec/model.py) 实现这些运算，测试另外用显式注意力方程和小型 Qwen3 作数值参照。

位置旋转还有一个直接的执行优化。记 $c_{ij}=\cos(i\omega_j)$、$s_{ij}=\sin(i\omega_j)$，
那么任一层向量的旋转就是 $x\odot c+\operatorname{rotate\_half}(x)\odot s$，再包含配置要求的幅度缩放。
c、s 由位置与固定频率确定，所有层共享同一份值。
因此一次前向中可以先算一份 c、s，让所有层的 Q、K 共用；位置旋转系数的计算次数从每层两次减少为整个前向一次。
对参数求导时 c、s 是相同的常数，因此共享计算得到相同梯度。
实现还保留独立旋转函数作参照；FP32、FP64、BF16 和 autocast 测试逐项核对 logits、KV、梯度及后段重放。

## 3. 同一个基座怎样变成草稿模型

把原线性层扩展成：

```math
f_\phi(x,m)=Wx+msBAx,\quad s=\alpha/r,
\qquad A\in\mathbb R^{r\times d_{in}},\ B\in\mathbb R^{d_{out}\times r}. \tag{5}
```

$m=0$ 关适配器，$m=1$ 开适配器。r 是秩，远小于输入、输出维度时，新增参数为
$r(d_{in}+d_{out})$，而原矩阵有 $d_{out}d_{in}$ 项。
低秩参数化来自 [LoRA](https://arxiv.org/abs/2106.09685)，按 token 开关用于隔离干净和带噪计算。

A 初始化为小随机矩阵、B 初始化为零，因此最初 $BA=0$。适配器接下来要学习根据前缀和噪声预测后续。
默认每层 Q、K、V、O、gate、up、down 七种投影都有适配器，草稿与验证共享基座。
起草时种子位置关，噪声位置开；验证时全部关。

若损失对层输出的导数为 g，链式法则给出：

```math
\nabla_B L=ms\,g(Ax)^\top,\qquad
\nabla_A L=ms\,B^\top gx^\top.                                      \tag{6}
```

初始化 $B=0$ 时，首步 A 的梯度为零，B 的梯度由 g 和 Ax 决定。数值微分测试通过对参数做小的正负扰动，
比较损失差商与自动求导，检验这个公式和实际低秩实现一致。

缩放系数 s 也出现在式 (6) 的梯度中，因此训练时同时记录秩 r 和分子 $\alpha$。
例如 r=32、$\alpha=32$ 时 s=1，$\alpha=512$ 时 s=16。`train --alpha` 设置分子，默认取 r；
检查点保存这两个值，评测入口按保存的配置构造适配器。测试覆盖 s=1、16 的数值微分与训练后权重存取。

### 3.1 在低秩空间门控，再融合投影与累加

将一批 token 的行向量排成矩阵 X，基座输出记为 O，令 M 为对角矩阵，其对角线是每行的布尔开关。
低秩分支为 $sMXA^\top B^\top$。矩阵结合律给出：

```math
Y=O+sM(XA^\top B^\top)=O+s(MXA^\top)B^\top.
```

因此先计算 r 维的 $H=XA^\top$，逐行乘 m，再由一次矩阵乘加得到 $Y=O+s(MH)B^\top$。
这把门控作用在 r 维特征上，并把 B 投影、缩放和累加交给同一次 `addmm`。
执行顺序参考[固定原仓库的低秩投影实现](https://github.com/ifm-ai/uno/blob/ed2ee36bb7a3aea8732ebc635b3f09490a032ea3/nano_vllm_uno/layers/linear.py)，
本项目在自有 `GatedLinear` 中实现上述公式。基础权重和 A/B 保持各自存储；后续参数更新直接用于下一次起草。

推理中的布尔门控 FP32／FP64 使用矩阵乘加路径；梯度计算采用式 (5) 的可微表达式，s=1 时合并逐元素门控与残差加法。
结合律证明描述实数运算；浮点计算按各自归约顺序舍入。测试覆盖 s=1、16、64、CPU／CUDA，比较融合前后结果并逐位检查关门控的基座行。
请求流吞吐和逐 token 比较在第 13 节报告。

## 4. 离散噪声、逆向后验与一步去噪

离散扩散用随机 token 逐渐替换原 token，再从上下文预测干净分布。
设原 token 是 x，噪声概率表为 $\pi$，保留率 $a_t\in[0,1]$ 随时间 t 下降：

```math
\Pr(z_t=v\mid x)=a_t\mathbf1[v=x]+(1-a_t)\pi_v.                       \tag{7}
```

$\mathbf1$ 为指示函数：条件成立取 1，其余情况取 0。实现方法是以概率 $a_t$ 保留 x，以概率 $1-a_t$ 按 $\pi$ 重抽。
重抽也可能抽回 x，因此最终与 x 相同的概率为 $a_t+(1-a_t)\pi_x$。
均匀 $\pi$、$a_t=0$ 对应从全词表抽纯噪声。

### 4.1 贝叶斯公式一步一步算回去

较干净时刻 s 和较噪时刻 t 满足 $a_s>a_t$，令 $\rho=a_t/a_s$。
从 $z_s$ 再以保留率 $\rho$ 做一次相同的替换，得到式 (7) 的边缘分布。
已观察到 $z_t=u$，中间 token 为 v 的后验是：

```math
\Pr(z_s=v\mid z_t=u,x)=
\frac{[\rho\mathbf1[v=u]+(1-\rho)\pi_u]
      [a_s\mathbf1[v=x]+(1-a_s)\pi_v]}
     {\sum_w[\rho\mathbf1[w=u]+(1-\rho)\pi_u]
      [a_s\mathbf1[w=x]+(1-a_s)\pi_w]}.                              \tag{8}
```

分子的第一项是“假设中间为 v，看到 u 的可能性”，第二项是“中间原本为 v 的可能性”。
分母对所有中间状态求和，使结果总和为 1。这就是有限词表中的贝叶斯公式。

实际生成时，用模型预测的软向量 $q_\phi$ 替代 x 的 one-hot 向量，定义插入式逆向核。
one-hot 输入对应精确贝叶斯后验；软向量输入对应这种选定的参数化。
插入式核先在式 (8) 中代入软向量，再对 v 归一化；分子和分母都使用代入后的值。

### 4.2 预测—校正混合为什么退化成一次前向

令 $R_{s,t}$ 表示式 (8) 的插入式核，$R_{0,t}$ 表示其中 $a_s=1$ 的干净预测。混合核写成：

```math
\Psi_{s,t}=\kappa R_{s,t}+(1-\kappa)[a_sR_{0,t}+(1-a_s)\pi],
\qquad 0\le\kappa\le1.                                               \tag{9}
```

两个概率向量的凸组合中，各项都在 $[0,1]$ 内，总和为 1。特别地，完全噪声一步回到干净端点 $a_t=0,a_s=1$ 时，
式 (8) 第一项在各个 v 上均为 $\pi_u$，归一化时约去这个共同因子，因此：

```math
R_{0,1}=q_\phi,\qquad\Psi_{0,1}=q_\phi.                              \tag{10}
```

因此主加速路径通过一次学生前向得到草稿分布。
[diffusion.py](../src/blockspec/diffusion.py) 实现式 (7)–(10)，测试穷举三项词表中的中间状态。

## 5. 离线训练：数据、注意力布局和蒸馏目标

干净数据为 $x_0,\ldots,x_{L-1}$，老师是冻结基座给出的下一项概率：

```math
p_j=p_\theta(\cdot\mid x_0,\ldots,x_j).                              \tag{11}
```

学生看干净前缀和带噪块，产生 $q_j$。两边相同位置 j 的 logits 都预测 j+1。
训练以老师的整张概率表作为监督。

### 5.1 同一遍前向的干净／带噪两条路径

输入拼成 $[x,z]$，长度为 2L，两边的位置编号重复 0 到 L−1。
定义块起点 $b(j)=B\lfloor j/B\rfloor$，读取规则为：

| 查询 | 可读的干净位置 | 可读的带噪位置 | 适配器 |
| --- | --- | --- | --- |
| 干净 j | $0\le k\le j$ | $\varnothing$ | 关 |
| 带噪 j | $k<b(j)$ | $b(j)\le k\le j$ | 开 |

$\varnothing$ 表示空集合。两边第一项 BOS（序列起始 token）都保持干净、关适配器；学生损失从位置 1 开始计算。
每个样本是一条独立的固定长度序列。老师在最后一行同样给出下一项分布，供该行蒸馏使用。

这使干净分支等于普通 AR 老师；每个学生块等于“先缓存此前的干净块，再单独算当前噪声块”。
测试逐项比较这两种布局的 logits。
[distillation.py](../src/blockspec/distillation.py) 明确构造 mask、位置编号和有效损失行。

### 5.2 四种差异函数

```math
\begin{aligned}
D_{TV}(p,q)&=\tfrac12\sum_v|p_v-q_v|,\\
L_1(p,q)&=\sum_v|p_v-q_v|=2D_{TV}(p,q),\\
D_{KL}(q\Vert p)&=\sum_vq_v\log(q_v/p_v),\\
D_{KL}(p\Vert q)&=\sum_vp_v\log(p_v/q_v).                             \tag{12}
\end{aligned}
```

TV 是总变差距离，描述两张概率表的质量差异；KL 是相对熵，方向由参数顺序决定。
代码区分 `l1`、`tv`、`reverse_kl`（学生到老师）、`forward_kl`（老师到学生）。
原研究的绝对差目标对应 `l1`，其值为 TV 的两倍。

此外，`reverse_kl_l1` 表示 $D_{KL}(q\Vert p)+L_1(p,q)$，共用同一对概率表计算。
这一组合取自[原论文式 (3)](https://arxiv.org/pdf/2609.04010) 的两项损失；本项目将两个系数设为 1 用于热身。
热身结束后保留 L1，学习率全程取给定值。测试核对联合损失及其梯度，老师分支使用停止梯度。

训练优化样本与随机噪声上的平均差异：

```math
\min_\phi\ \mathbb E_{x,z}
\left[\frac1{L-1}\sum_{j=1}^{L-1}D(p_j,q_{\phi,j})\right],\qquad\theta\text{ 固定}. \tag{13}
```

学生有梯度，老师停止梯度。支持 2、4、6、8 等小块到大块的课程；当前训练默认使用反向 KL＋L1 热身，之后改为 L1。
还可选择纯 KL、前向 KL 热身或单一目标。热身计入总步数，块长按训练进度依次切换。
数据窗口与噪声各用独立随机数生成器。[training.py](../src/blockspec/training.py) 提供课程入口。

课程列表为 $B_0,\ldots,B_{J-1}$、总步数为 T 时，从零计数的第 t 步使用 $B_{\lfloor tJ/T\rfloor}$。
短块让学生先学习较近的后续，后面的阶段逐步加入更远位置。每一步仍按 5.1 构造长度为 2L 的配对输入。
课程的效果由固定验证和生成评测共同检查；块长对应的注意力布局决定学生在各阶段接收的上下文。

训练入口支持固定裁剪、固定噪声的独立验证集，记录给定真实前缀时的 TV、前向 KL 和 argmax 一致率。
训练前检查问题与样本划分；验证以固定权重前向计算，窗口及噪声有独立指纹。

蒸馏默认比较温度 1 的全词表软分布，以便获得梯度。
后文“接受率 = 1−TV”的恒等式使用实际生成时的 p、q。
训练与生成采用相同的概率处理时，训练 TV 直接对应单位置接受率；在贪心或其他温度／截断设置下，它作为可微代理目标。

### 5.3 初始梯度与训练数据的作用

softmax 的导数为 $\partial q_i/\partial l_j=q_i(\mathbf1[i=j]-q_j)$。代入式 (12)：

```math
\frac{\partial D_{KL}(p\Vert q)}{\partial l_j}=q_j-p_j,\qquad
\frac{\partial D_{TV}(p,q)}{\partial l_j}
=\tfrac12q_j\left[s_j-\sum_iq_i s_i\right],\quad s_i=\operatorname{sign}(q_i-p_i). \tag{14}
```

当老师概率 $p_j$ 较大而学生概率 $q_j$ 很小时，TV 梯度受 $q_j$ 缩放，前向 KL 的梯度则约为 $-p_j$。
等号位置 TV 选取合法次梯度。这给出了热身的梯度依据，热身配置通过接受长度和总时间比较。

记 $R=\sum_iq_i\log(q_i/p_i)$，反向 KL 的导数为 $q_j[\log(q_j/p_j)-R]$。
因此联合热身对第 j 个 logit 的导数是
$q_j[\log(q_j/p_j)-R+s_j-\sum_iq_i s_i]$，绝对值项对应 L1。
联合热身同样带 $q_j$ 因子；它与前向 KL 的梯度特性构成两种热身选择。

老师读取块内真实历史，学生读取块内噪声。对固定学生可见信息 c，最小化平均前向 KL 的解为：

```math
q^*(v\mid c)=\mathbb E[p(v\mid\text{老师历史})\mid c].                \tag{15}
```

推导：把平均 KL 写成 $C-\sum_v\bar p_v\log q_v$，其中 C 对 q 的导数为零。
加上约束 $\sum_vq_v=1$ 的乘子 $\lambda$，求导得到 $-\bar p_v/q_v+\lambda=0$，
求和得 $\lambda=1$，所以 $q=\bar p$。同一可见前缀有多种合理后续时，最优学生学习的是这些条件分布的平均。

因此数据决定了“在哪些上下文模仿老师”。离线数据越贴近线上上下文，蒸馏学到的条件分布就越有针对性。

## 6. 一轮两遍的解码对齐

已提交历史 h 长度为 L，最后一项叫种子 s。缓存保存前 L−1 项，s 在本轮计算。
块长 B 包含一个干净根 token 和 B−1 个噪声起草位置：

| 步骤 | 输入 | 输出概率的含义 | 留下的状态 |
| --- | --- | --- | --- |
| 起草 | `[s,z1,…,z(B−1)]` | 第 0 行给根；第 i 行给候选 i | 干净 s 的 KV |
| 验证 | `[y0,y1,…,y(B−1)]` | 第 i−1 行验证 yi；最后一行给全接受时额外 token | 基座真实路径 KV |
| 更新，可选 | 起草输入、原缓存、有效老师 logits | 重算学生梯度 | 下一轮的适配器版本 |

根 $y_0$ 从适配器关的第 0 行抽，因此已经服从目标。后续 $y_i\sim q_i$ 来自带噪位置，
条件于本轮噪声独立抽样。老师读取实际候选路径：
$p_i=p_\theta(\cdot\mid h,y_0,\ldots,y_{i-1})$。

z 是学生输入，y 是从学生输出分布抽出的候选。线上重放保存 z，以恢复相同的学生输入。
[decoding.py](../src/blockspec/decoding.py) 实现这一对齐，并提供逐 token 一次前向的 AR 基线。

## 7. 概率校正如何得到目标分布

候选 $Y\sim q$，再抽独立均匀数 $U\in[0,1)$，按下式接受：

```math
a(Y)=\min\{1,p_Y/q_Y\},\qquad U<a(Y).                               \tag{16}
```

若拒绝，从残差分布抽替代 token：

```math
r_v=\frac{(p_v-q_v)_+}{Z},\qquad Z=\sum_u(p_u-q_u)_+,
\qquad a_+=\max(a,0).                                                \tag{17}
```

这是 [精确投机解码](https://proceedings.mlr.press/v202/leviathan23a.html) 的关键校正。下面重新证明。
直接接受 v 的概率为 $q_va(v)=\min(p_v,q_v)$。因为两张表总和相同，正差与负差总量相同：

```math
\sum_v(p_v-q_v)_+=\sum_v(q_v-p_v)_+
=1-\sum_v\min(p_v,q_v)=D_{TV}(p,q)=Z.                                \tag{18}
```

因此总拒绝概率为 Z，最终输出 v 的概率正好是：

```math
\Pr(\text{输出 }v)=\min(p_v,q_v)+Zr_v
=\min(p_v,q_v)+(p_v-q_v)_+=p_v.                                     \tag{19}
```

例如 $p=(0.2,0.5,0.3)$、$q=(0.6,0.1,0.3)$，直接接受质量是 $(0.2,0.1,0.3)$，
总拒绝质量 0.4，残差分布将这部分质量补给 B，最终就得到 p。

当 $q_v=0$ 时，候选 v 的抽样概率为零；残差分支承担其目标概率质量。
当 p=q 时 Z=0，所有候选直接接受。

### 7.1 多步、EOS 与贪心

按顺序验证，第一处拒绝后发出替代 token 并结束本轮，更深的候选和对应 logits 随本轮丢弃。
全部接受时，从最后一行额外抽一项，此时它的条件正是真实提交的历史。
对第一项应用式 (19)，条件于第一项再对第二项应用，逐项归纳就得到式 (2)。
EOS 与长度预算截取生成序列的一个前缀，该前缀也服从相应的目标边缘分布。

贪心时 p 为 one-hot，错误候选必被换成正确贪心项，故精确算术下与 AR 逐 token 一致。
GPU 批形状和运算顺序带来的浮点舍入，可能交换数值接近的两个 logits；实验同时记录数值误差与贪心一致率。

## 8. 缓存隔离的证明

KV cache 保存每层历史位置的键和值，后续直接读取这些结果。

每轮开始时，缓存满足一致性条件：它等于固定基座、适配器全关计算已提交历史前 L−1 项的结果。
种子 s 的适配器关，读取范围为干净历史和 s 自身。逐层归纳：其嵌入由 s 确定；前层干净表示和缓存由历史与基座确定；
下一层由这些量继续计算。因此种子的所有 KV 都等于在同一历史下单独输入该种子所得的基座结果，可以接入历史缓存；噪声位置的 KV 随本轮释放。

验证时适配器全部关闭，提交首次拒绝之前的合法路径 KV。替代 token 或全接受后的额外 token 成为下一轮种子，
其 KV 留到下一轮计算。这样缓存始终保存至“新历史倒数第二项”，满足下一轮的一致性条件。
全接受、第一处拒绝、部分接受、EOS、短尾都有测试。

证明使用两个条件：基座权重固定，缓存全部来自适配器关闭的计算。任意层的适配器都可以在此条件下继续更新。
默认路径采用函数式缓存和 `torch.cat`；可选 GPU 图路径在内部复用固定工作区，返回独立存储的缓存快照，
见下一节。重放容量由块数配置，实际占用由前缀长度及切片持有的底层存储共同决定。

### 8.1 固定工作区与反馈快照

GPU 图可以记录一段固定形状的运算，后续向同一地址放入新输入，再一次提交这段运算，减少逐个算子的 CPU 调度。
执行器固定地址、形状和控制流，并显式管理参数版本和反馈快照。
这些约束见 [PyTorch 2.11 CUDA 图说明](https://docs.pytorch.org/docs/2.11/notes/cuda.html#cuda-graphs)。
[execution.py](../src/blockspec/execution.py) 在 `Decoder` 外实现这一执行层。

设真实历史有 l 个已缓存位置，新输入有 n 项。选择固定历史容量 P，满足 $P\ge l$。
物理存储为“P 格历史＋n 格本次输入”，历史区域的第 l 到 P−1 格用作填充位。
把原来逻辑上的键索引 j 映射为：

```math
f(j)=\begin{cases}j,&0\le j<l,\\P+j-l,&l\le j<l+n.\end{cases}
```

真实历史与本次输入的 K、V 按 f 放置；这些位置的读取关系与原 mask 相同，填充位的注意力权重设为零。
位置编码使用逻辑位置，例如新输入的第一项位置为 l，物理存储偏移为 P。
令 $a_{ij}$ 为式 (3) 中允许位置的注意力分数，原来给 j 的权重是
$e^{a_{ij}}/\sum_{k\text{ 允许}}e^{a_{ik}}$。填充位对分母的贡献为 $e^{-\infty}=0$，
所以 f(j) 的权重等于原 j 位置的权重，乘上相同的 V 后输出也相同。逐层归纳，得到相同的 logits 和有效 KV。
验证结束再按逆映射提取有效位置，提交合法路径。上述推导使用实数运算，浮点实现通过数值对照检验。

这里还有一个与在线学习直接相关的所有权问题。假设第 t 轮工作区为 A，若反馈保存的是 A 的视图，
第 t+1 轮向 A 写新 KV，就同时改变了旧训练样本。`detach()` 与原张量共享存储。
因此本实现每次返回的有效 KV 经一次独立复制／拼接成为 $S_t$；logits 和边界特征也复制。
下一轮在私有 A 中计算，重放继续持有第 t 轮的 $S_t$，满足 9.3 的输入一致性条件。
`PackedCache` 把各层 KV 放在一个张量中，支持一次整体复制，保持相同的缓存内容与提交规则。
测试复用同一个工作区、缩短历史、修改适配器，再对旧反馈做反向，逐项检查旧快照。

所有短查询形状在请求计时前准备，入口核对查询与形状配置。长 prefill 经同一模型普通执行。
在线更新按“验证、提交、反向计算、AdamW”执行，原地更新低秩矩阵 A、B，下一轮图读取同一地址中的新值。
替换参数存储、修改旋转缓冲区、切换精度或注意力后端后，重新准备执行器。
当前执行配置为单请求、单所有者、同步更新和 FP32 CUDA。

### 8.2 按新增位置打包 KV

每层注意力的输出缓存由“输入历史＋本次新 KV”组成。记输入工作区为 $K_j^{past}$，新增部分为 $K_j^{new}$，
下标 j 表示层；值缓存 V 具有相同结构。历史工作区有 P 格、实际使用 l 格，因此：

```math
\operatorname{select}_{[0,l)\cup[P,P+n)}
\bigl([K_j^{past},K_j^{new}]\bigr)
=[K_j^{past}[0:l],K_j^{new}].
```

左边从完整输出缓存提取有效位置，右边直接取输入工作区的有效历史，再拼上新增位置。
两者包含相同的数值，历史部分原样复制。实现按层堆叠新增 KV，将它们排进一个紧凑张量，
随后与历史工作区的有效部分拼接，生成独立存储的返回快照。
注意力、位置编码和词表投影保持原计算，旧反馈继续持有各自的缓存副本。

28 层、8 个 KV 头、每头 64 维、FP32、P=768、n=4 时，完整输出的打包张量为 84.44 MiB，
新增 KV 的打包张量为 0.4375 MiB。返回缓存的长度仍为 l+n；节省来自图内部堆叠张量的行数。
CPU／CUDA 测试覆盖空历史、短历史、满容量历史和适配器开关，核对 logits、完整缓存、历史逐位一致及快照跨轮有效性。

同一执行器的各查询形状共享一份历史输入工作区。每次调用先复制有效前缀，再运行相应图，最后生成独立返回快照。
设第 t 次调用为 $F_t(H_t,X_t)$，写入工作区的有效部分始终为 $H_t$，注意力 mask 指定其余位置的可见性，
因此本次输出只取决于本次历史与查询。串行调用与独立输出快照保证下一次写入保持旧返回值。
这项改动把 G 个图槽的历史输入存储从 G 份降到 1 份；中间激活和返回快照按各自生命周期持有。
测试交替调用带适配器与普通形状，核对共享地址、前向结果和旧缓存的逐位稳定性。

### 8.3 共享 K/V 的分组短查询计算

模型可以让多个查询头共享一组键和值。设查询头有 $H_q$ 个、KV 头有 $H_k$ 个，
每个 KV 头服务 $G=H_q/H_k$ 个查询头。当前真实基座为 $H_q=32,H_k=8,G=4$。
一次前向有 L 个查询位置、T 个键位置，每头维度为 d。第 h 个查询头读取第 $\lfloor h/G\rfloor$ 个 KV 头。
SDPA 对照将每组 K/V 复制 G 份后按头计算；对应语义见 [PyTorch 的注意力定义](https://docs.pytorch.org/docs/2.11/generated/torch.nn.functional.scaled_dot_product_attention.html)。

分组实现把共享 K/V 的查询排进同一个矩阵。用 k 表示 KV 头，g 表示组内查询头，i 表示查询位置，定义行号
$r=gL+i$，并令：

```math
\widetilde Q_{k,gL+i,:}=Q_{kG+g,i,:},\qquad
\widetilde S_k=\frac{\widetilde Q_k K_k^\top}{\sqrt d}.
```

这是一次行的重新排列。将分数恢复成“查询头、查询位置、键位置”的形状后，
每一项仍为 $S_{h,i,t}=\langle Q_{h,i,:},K_{\lfloor h/G\rfloor,t,:}\rangle/\sqrt d$。
使用相同的可见位置集合 $\mathcal A_{h,i}$，注意力概率和输出为：

```math
A_{h,i,t}=\frac{e^{S_{h,i,t}}}{\sum_{u\in\mathcal A_{h,i}}e^{S_{h,i,u}}},\quad t\in\mathcal A_{h,i},
\qquad O_{h,i,:}=\sum_{t\in\mathcal A_{h,i}}A_{h,i,t}V_{\lfloor h/G\rfloor,t,:}.
```

概率沿键位置归一化，每行读取同一组 K/V，因而输出恢复头顺序后与按头复制的计算相同。
空可见集合的输出定义为零；实现先给该行一个常数分数，再把其所有概率置零，使前向和梯度均为零。
因果、树形、填充以及每个头各自的 mask 都遵循这个定义。

梯度也可以直接核对。记 $\bar S=\partial\mathcal L/\partial S$、$\bar O=\partial\mathcal L/\partial O$，
一个共享键和值接收组内所有查询的贡献：

```math
\frac{\partial\mathcal L}{\partial K_{k,t,:}}
=\frac1{\sqrt d}\sum_{g=0}^{G-1}\sum_{i=0}^{L-1}\bar S_{kG+g,i,t}Q_{kG+g,i,:},
\qquad
\frac{\partial\mathcal L}{\partial V_{k,t,:}}
=\sum_{g=0}^{G-1}\sum_{i=0}^{L-1}A_{kG+g,i,t}\bar O_{kG+g,i,:}.
```

这里的求和正好等于复制操作反向时归并各份 K/V 梯度的结果；Q 的梯度则按原来的行号取回。
因此在实数运算下，重新排布同时保持前向函数和参数梯度，末层在线重放可以使用同一计算。

[attention.py](../src/blockspec/attention.py) 用矩阵乘法、mask 与 softmax 实现上述过程。
显式扩展的 K/V 含 $2bH_qTd$ 项，分组路径复用原有的 $2bH_kTd$ 项，分数张量含 $bH_qLT$ 项，b 为 batch 大小。
当前 `grouped` 后端用于 L≤32 的 FP32／FP64 查询；长 prefill 和低精度查询继续使用 SDPA。
该选择利用短查询的分数矩阵规模，在 AR、起草、树验证和末层训练中保持同一执行规则。
后端在准备 GPU 图和创建 learner 前设置，切换后重新构造图与反馈。

测试以 FP64／FP32 比较前向、Q/K/V 梯度，并进行有限差分检查；线性和树解码测试进一步比较跨请求的输出、
接受数、适配器参数、Adam 状态和末层梯度图。完整基座的外部数值对照与吞吐见第 13 节。

## 9. 在线续训：输入、目标和更新

线上主题、风格、长度可能偏离离线数据。可以用刚完成的验证 logits 继续训练草稿，免去额外教师前向。
用验证反馈训练提议模型的相关工作包括 [Online Speculative Decoding](https://arxiv.org/abs/2310.07177)
和 [Test-Time Speculation](https://arxiv.org/abs/2605.09329)。这里独立实现同步重放，保留全适配器基线，
并提供下文推导的末层子集续训选项。

### 9.1 哪些位置有有效监督

每个反馈保存：干净种子与噪声输入、种子之前的基座 KV、老师 logits。
如果接受 k 个噪声候选，第 k+1 个拒绝，保留前 k+1 行；全部接受则保留全部 B−1 行。
首次拒绝位置的老师历史等于已提交前缀，因此该位置也纳入监督。
EOS 或长度预算结束生成时，监督行按返回前缀截取；生成中的请求按配置触发更新，时点位于本轮提交与下轮起草之间。

### 9.2 全适配器重放与更新

维护最多 R 个反馈块，每 S 轮最多更新一次。反馈集合为 $\mathcal B_t$，$m_{bi}$ 指示有效位置：

```math
L_t(\phi)=\frac{\sum_{b\in\mathcal B_t}\sum_i
 m_{bi}D(p_{bi},q_\phi(\cdot\mid c_b,z_b)_i)}{\sum_{b,i}m_{bi}},
\qquad \phi\leftarrow\operatorname{AdamW}(\phi,\nabla_\phi L_t).       \tag{20}
```

老师停止梯度；保存的反馈组成经验训练集，学生使用当前参数重新前向，建立计算图并反传。
各反馈块依次处理、累积梯度，再执行裁剪和参数更新；激活内存按一个块的规模使用。

默认 R=4、S=16、学习率 $10^{-4}$、L1 损失，继续训练离线检查点中的 A、B。
[online.py](../src/blockspec/online.py) 实现学生前向、backward 和 optimizer step。
同一 learner 的权重和 Adam 状态跨请求保留，请求结束释放反馈和 KV。
检查点保存权重；重建 learner 时初始化 Adam 状态。

### 9.3 末层续训与特征复用

离线训练覆盖所有层的适配器。末层在线方案训练最后 k 层中的原有 A、B，并固定前面层的适配器。
例如 28 层模型取 k=4，继续训练原检查点第 25–28 层的低秩矩阵。
`OnlineConfig(train_last_layers=4)` 开启这个选择；默认的 `None` 对应 9.2 的全适配器续训。

将前段适配器写成 $\phi_{<j}$，可训练后段写成 $\psi=\phi_{\ge j}$。对一个已保存的种子和噪声输入，
前 j 层产生隐藏向量 H，剩余层及词表投影产生 logits $\ell$：

```math
H=F_{<j}(c,z;\theta,\phi_{<j}),\qquad
\ell=G_{\ge j}(H,K^\theta;\psi),\qquad q=\operatorname{softmax}(\ell). \tag{20a}
```

$K^\theta$ 表示后段所需的干净基座历史缓存。H 直接取自本轮起草前向，包含种子和噪声各位置的特征。
位置编号、注意力 mask 和适配器开关一同保存。

为什么可以安全断开 H 的梯度？导数描述“轻微改变参数时，结果如何变化”。对于一组参数，
把每一项这样的导数排成向量就是梯度；矩阵 $\partial G/\partial H$ 收集输出各项对输入各项的导数。
链式法则把“参数直接改变后段”和“参数先改变 H，再经后段影响损失”两条作用相加。
令 $g=\partial L/\partial\ell$，则完整重算给出的梯度是：

```math
\nabla_\psi L
=\left(\frac{\partial G}{\partial\psi}\right)^\top g
+\left(\frac{\partial H}{\partial\psi}\right)^\top
  \left(\frac{\partial G}{\partial H}\right)^\top g
=\left(\frac{\partial G}{\partial\psi}\right)^\top g.                 \tag{20b}
```

最后一步成立，是因为 H 由已冻结的前段产生，$\partial H/\partial\psi=0$。
因此给定相同数据、当前 $\psi$ 和相同损失，复用 H 并重算后段，得到的正是
**相同末层参数子集**在完整重算中应得的梯度。
裁剪对象、Adam 初始状态及超参数也相同时，一步更新相同；逐步归纳得到多次更新相同。
这个选择将适应新分布的任务交给后 k 层，k 决定了可训练范围和计算成本。

这里有三条独立的有效性条件：

- H 对应的前段基座和适配器保持固定，H 可跨后段的多次更新复用。
- 重放历史 KV 来自适配器关闭的基座前向。
- H 的噪声、位置和读取关系与采集反馈时一致。

实现保存后 k 层历史 KV 和分界特征副本，检查前段参数版本与冻结状态。
前段发生修改时重新构造 learner；请求结束释放反馈数据。

验证时完整重算同一前段冻结的模型，比较所有可训练梯度。适配器 A、B 均赋随机权重，三种切分下 logits 和梯度一致，
连续三次 Adam 更新在 FP64 的 $10^{-14}$ 容差内一致，冻结参数逐位相同。
线性与树解码检查基座／前段权重保持固定、末段参数更新及贪心输出一致性。
接受长度和计算成本通过 13.3 的请求流评测比较。

### 9.4 按监督位置计算词表投影

词表投影把每个隐藏向量转换为 V 个 logits。本机模型的 V=64,256，在线反馈块通常只监督几个位置。
实现让 Transformer 计算完整块的隐藏状态，然后选出有效监督行，再执行最终 RMSNorm 和词表投影。
历史 KV、位置编号和块内注意力关系保持原布局。

设最后一层隐藏状态为 $H\in\mathbb R^{L\times d}$，共有 L 行、每行 d 个数；
S 是从 L 行中抽取 m 行的选择矩阵，N 表示逐行 RMSNorm，$W\in\mathbb R^{V\times d}$ 是词表权重。
RMSNorm 的每行计算只使用这一行的数值，因此 $S N(H)=N(SH)$。结合矩阵乘法的结合律：

```math
S\bigl(N(H)W^\top\bigr)
=\bigl(S N(H)\bigr)W^\top
=N(SH)W^\top.                                                    \tag{20c}
```

左边先计算全部 logits 再选行，右边先选隐藏向量再投影，两者给出相同的监督 logits。
只要损失由这些行构成，两条表达式表示同一个参数函数；对任意可训练适配器参数求导，梯度也相同。
词表乘法的行数由 L 降为 m，乘加规模从约 $2LdV$ 降为 $2mdV$。
Transformer 层仍处理完整块，梯度通过块内注意力传播到所有参与监督预测的隐藏位置。

`logit_range=(1, 1+valid)` 选择种子之后的有效位置。全量与末层重放共用这个入口。
CPU／CUDA 测试比较 logits、KV 和全部可训练参数的梯度：FP32 容差 $10^{-6}$、FP64 容差 $10^{-12}$，KV 逐位相同。
区间选择包含单行、多行及边界位置；矩阵形状变化产生的舍入差异由上述数值检查覆盖。

### 9.5 融合在线参数更新

在线训练沿用式 (20) 的老师分布和有效位置。`forward_kl` 对应 $D_{KL}(p\Vert q)$，
单个位置对学生 logit $\ell_v$ 的梯度为 $q_v-p_v$：老师分配较多概率的项获得相应的提升方向。
离线起点和在线损失可分别配置，当前请求流实验在离线 L1 起点上继续做前向 KL 更新。

Adam 为每个参数保存梯度的一阶平均 m 和平方梯度平均 v。令 $g_t$ 为裁剪后的第 t 次梯度，
当前实现采用零权重衰减，则更新为：

```math
m_t=\beta_1m_{t-1}+(1-\beta_1)g_t,\qquad
v_t=\beta_2v_{t-1}+(1-\beta_2)g_t^2,
\qquad
\phi_{t+1}=\phi_t-\eta
\frac{m_t/(1-\beta_1^t)}{\sqrt{v_t/(1-\beta_2^t)}+\epsilon}.         \tag{20d}
```

平方、平方根和除法逐参数进行；$\beta_1=0.9,\beta_2=0.999$ 控制历史梯度保留比例，$\epsilon=10^{-8}$ 稳定分母。
融合实现将多个参数张量及多步逐元素运算合并到 GPU 内核中，减少内核启动和中间读写。
具体执行接口见 [PyTorch 2.11 AdamW](https://docs.pytorch.org/docs/2.11/generated/torch.optim.AdamW.html)。

`optimizer="auto"` 在 CUDA 上选择融合版本，CPU 上选择标准版本；`standard` 和 `fused` 可显式指定。
两种实现使用相同损失、学习率、梯度裁剪和 Adam 超参数。
CUDA 测试连续执行四次更新，逐项比较损失、参数和一阶／二阶状态；全量与末层续训均通过。

### 9.6 按更新需求安排反馈窗口

设每 S 个验证轮次更新一次，每次使用最近 R 个反馈块。在两次更新之间，较早保存的反馈会被最近 R 个覆盖。
可以预先计算哪些轮次会进入更新，把特征保存、老师 logits 收集和反馈复制安排在这些轮次。
例如 S=8、R=1 时，采集第 8、16、24…轮；起草、验证与轮次计数照常推进。

这里 t 表示带有有效老师监督的验证轮次编号，下一次更新时点为 $u=S\lceil t/S\rceil$。
其中 $\lceil\cdot\rceil$ 表示向上取整，mod 表示除法的余数。
“距离更新还有几轮”包含本轮，因此为 $u-t+1$。采集条件为：

```math
I_t=\mathbf1[u-t+1\le R]
=\mathbf1\bigl[S-((t-1)\bmod S)\le R\bigr].                       \tag{20e}
```

当 R≥S 时，每轮都满足条件。考虑 R<S：若当前请求从第 a 轮开始，逐轮采集在第 u 轮实际用于更新的反馈为
$\max(a,u-R+1),\ldots,u$。这些轮次全部满足式 (20e)，更早的反馈在更新时已被队列覆盖。
窗口化采集因此在每个更新时点得到相同的、有相同顺序的训练样本。
请求结束释放重放内容，跨请求保留参数、Adam 状态与轮次计数；新请求从自己的 a 重新积累。
单 token 收尾由普通 AR 处理。完成请求的轮次提交输出并清理重放，下一请求继续使用已有计数。

再对更新次数归纳：初始参数和 Adam 状态相同；若某次更新前相同，使用相同反馈重算便得到相同损失和梯度，
更新后参数与状态也相同。固定两边的随机数流，下一轮起草和验证的数学结果随之相同。
该推导沿用实数运算，FP32 执行通过参数与生成对照核验。

`feedback_execution="windowed"` 在线性／树解码中启用窗口，`all` 提供逐轮采集对照。
两种解码器每个进入反馈计数的轮次都具有至少一个有效监督位置，跳过采集的入口检查这一条件。
通用 `observe()` 接口仍逐次接收调用方给出的反馈，支持带空目标的反馈序列与手动更新。
窗口外的起草使用普通适配器图，窗口内使用包含分界特征的图；全适配器续训共用同一套普通适配器图。
准备成本按各方法实际需要的图形状集合计入。

每 S 轮采集 $\min(R,S)$ 个反馈块，`feedback_blocks` 报告实际数量。
单元测试逐次比较训练样本、梯度、参数和 Adam 状态，覆盖 R<S、R=S、R>S、请求短尾、EOS 与随机采样。
该采集方式也适用于下一节的覆盖触发策略；真实基座的联合对照见 9.7。

### 9.7 根据实际路径覆盖安排更新

一轮起草给出 B−1 个带噪位置。线性解码的 K 是接受的候选数，树解码的 K 是实际路径匹配的子节点数。
当 K=B−1 时，记这一轮为完整覆盖。覆盖标记衡量实际生成路径；式 (20) 衡量整张概率表的差异。
在线策略用覆盖标记分配学习时间：在每 S 轮的检查时点，完整覆盖的窗口保留当前参数，
包含覆盖缺口的窗口执行一次训练，使用窗口中全部有效位置的老师软分布。

令 $c_b=\mathbf1[K_b=B_b-1]$，其中指示函数 $\mathbf1[\cdot]$ 在括号成立时取 1、其余取 0。
令 $s=(\phi,m,v,j)$ 收集适配器、Adam 两种平均值和实际更新次数，$U(s,\mathcal B)$ 表示式 (20) 的一次完整更新。
对一个具有反馈、且请求继续生成的检查时点，有：

```math
G=1-\prod_{b\in\mathcal B}c_b,\qquad
s_{next}=\begin{cases}
U(s,\mathcal B),&G=1,\\
s,&G=0.
\end{cases}                                                     \tag{20f}
```

乘积在窗口内每个标记都是 1 时等于 1，所以此时 G=0，状态原样保留；任一标记为 0 时 G=1。
例如 R=2 时，标记 (1,1) 保留状态，(1,0) 和 (0,1) 都训练两个反馈块的全部有效位置。
窗口选择改变了在线经验数据的采样权重。它将学习预算集中到存在覆盖缺口的窗口，收益通过请求流实测核算。

实现通过 `update_policy="coverage"` 启用，`periodic` 为默认周期更新对照。
标记由已经完成的验证产生，重放时视为常量。G=0 时直接返回，适配器、已有梯度、Adam 平均值和实际步数均原样保留；
反馈轮次计数继续推进。式 (20d) 的偏差校正使用实际更新次数 j，请求结束保留权重与 Adam 并释放反馈。
`Feedback.fully_covered` 默认为 False；解码器提供实际匹配结果，True 标记配套保存全部 B−1 行老师目标。

这套门控与窗口采集的组合也有逐步等价性：9.6 证明两种采集方式在检查时点具有相同反馈，
反馈中的覆盖标记也相同，因此 G 相同。若 G=0，两边保留同一状态；若 G=1，相同梯度交给相同 Adam 更新。
由初始状态开始归纳，每个检查时点后的状态相同，下一轮提议随之相同。目标分布的保持由第 10 节的自适应提议证明保证。

测试逐项检查完整覆盖窗口的参数、梯度和 Adam 状态；混合窗口继续监督全部有效位置。
CPU／CUDA 的线性与树解码覆盖完整路径、EOS 和跨请求计数；全量／末层两种续训均通过窗口等价检查。
真实基座采用 13.2 的 1,200 步起点，验证文件第 1、5 条记录各取 256 项前缀、生成 512 token，种子分别为 271828、271829。
B=4、top-k=8、树预算 12、S=8、R=1，最后 4 层以前向 KL 和学习率 $3\times10^{-4}$ 续训，使用 FP32 CUDA 图。
逐轮采集与窗口采集依次从相同初始参数、全新 Adam 开始，反馈块由 283 降到 35，输出及逐轮接受数相同；
两个请求分别执行 10、12 次更新，并保留状态 6、7 次。逐请求参数最大绝对差约 $3.48\times10^{-6}$，
Adam 一阶／二阶状态分别约为 $4.66\times10^{-8}$、$8.19\times10^{-11}$，通过预设的绝对／相对 $10^{-5}$ 容差，基座指纹相同。

### 9.8 固定形状的末层梯度重放

末层续训的一次计算由隐藏特征、历史 KV、注意力读取关系、老师 logits 和有效位置数决定。
[replay_execution.py](../src/blockspec/replay_execution.py) 为每个“块长 B、有效位置数 m”准备一张执行图，
录制末层前向、式 (20) 的概率损失及适配器梯度。运行时把当前反馈复制到固定地址，重放整组 GPU 运算。
图外继续执行有限值检查、梯度裁剪和相同的 Adam 更新；覆盖门控沿用 9.7。
固定地址、固定形状和图外同步的执行约束见 [PyTorch 2.11 CUDA 图说明](https://docs.pytorch.org/docs/2.11/notes/cuda.html#cuda-graphs)。

历史长度 l 随生成增长，图使用固定容量 P。把 l 个真实 KV 放在前 l 行，新块放在 P 之后，
中间 P−l 行通过注意力 mask 排除。对一个查询，真实键的分数记为 $a_j$，可读取集合记为 A：

```math
o=\frac{\sum_{j\in A}e^{a_j}v_j}{\sum_{j\in A}e^{a_j}}
=\frac{\sum_{j\in A}e^{a_j}v_j+\sum_{j\in padding}0\,v_j}
{\sum_{j\in A}e^{a_j}+\sum_{j\in padding}0}.                       \tag{20g}
```

padding 行的注意力权重为零，真实位置沿用原来的位置编号和读取关系，因此输出相同。
逐层应用该等式，得到相同的 logits 和损失函数；对相同末层参数求导，梯度也相同。
这是实数运算下的等式，有限精度实现通过普通路径与固定容量路径的梯度对照核验。

一张图计算一个反馈块的梯度。令窗口中总有效位置数为 $M=\sum_b m_b$，则：

```math
g_b=\nabla_\psi\left[\frac1M\sum_{i=1}^{m_b}D(p_{bi},q_{bi}(\psi))\right],
\qquad g=\sum_{b\in\mathcal B}g_b.                                \tag{20h}
```

每个图使用当前 M；分母因此按监督位置总数归一化。所有反馈计算完后，对总梯度 g 做一次裁剪，再更新一次 Adam。
图输出与累计梯度分别存储：窗口开始清零累计区，每次重放把 $g_b$ 加进去。
这使同一图连续使用多次时，前面反馈的贡献继续保留。录图计算梯度并保留张量存储，适配器、已有 `.grad` 与 Adam 状态保持原样。
运行入口校验模型存储、可训练层范围、位置编码缓冲区、反馈形状、精度和容量。

图准备在请求计时前完成，按在线方法所需形状计入一次性准备成本。B 最大为 4 时，
为 (2,1)、(3,1)、(3,2)、(4,1)、(4,2)、(4,3) 六种签名准备梯度图。
同一个准备好的执行器可供同一模型的后续 learner 使用，每个 learner 持有自己的 Adam 状态。
CPU／CUDA 测试覆盖多种块长、监督长度、历史长度、重复签名和连续四次更新，逐项比较损失、梯度、参数和 Adam 状态。

执行器单向持有图槽，图槽持有计算所需的模型和张量。最后一个执行器引用释放后，图槽与私有工作区随之释放。
CPU／CUDA 生命周期测试暂时关闭自动循环回收，通过弱引用检查对象释放，并确认 CUDA 已分配显存下降。
PyTorch 的张量占用与分配器保留空间分别由 `memory_allocated()` 和 `memory_reserved()` 统计，
缓存空间可供后续计算复用，含义见 [显存管理说明](https://docs.pytorch.org/docs/2.11/notes/cuda.html#memory-management)。

分组路径下，真实基座沿 9.7 的两个请求检查 22 次更新。在每次更新前固定同一参数与反馈，普通重放与梯度图的
原始梯度最大相对 L2 差异为 $2.82\times10^{-6}$、最大逐项绝对差为 $2.29\times10^{-5}$，损失最大绝对差为 $6.68\times10^{-6}$，
通过预先给定的梯度相对 L2 $10^{-4}$、损失绝对差 $10^{-4}$ 门槛。
其中相对 L2 差异指 $\|g_{graph}-g_{eager}\|_2/\|g_{eager}\|_2$；$\|g\|_2$ 先将各分量平方求和，再开平方，衡量整个梯度的大小。
两条独立学习轨迹的生成 token、逐轮接受数和更新次数相同；累计参数最大绝对差为 $2.08\times10^{-5}$，
Adam 一阶／二阶状态分别约为 $1.54\times10^{-7}$、$4.13\times10^{-10}$，基座指纹相同。
这些数值记录固定容量布局和普通布局的 FP32 执行差异；小模型数学对照采用 FP64 $10^{-12}$、CUDA FP32 $2\times10^{-6}$ 容差。

梯度误差与参数误差的尺度还受 Adam 归一化影响。例如首步对裁剪后的单个梯度 g 使用
$f(g)=g/(|g|+\epsilon)$，其导数为 $\epsilon/(|g|+\epsilon)^2$。
当 |g| 接近零时，导数接近 $1/\epsilon$；学习率 $3\times10^{-4}$、$\epsilon=10^{-8}$ 对应参数更新的局部放大系数可达 $3\times10^4$。
因此验证并列记录梯度、Adam 状态、累计参数和实际生成轨迹。

## 10. 在线参数更新的正确性证明

令 $\mathcal F_t$ 包括第 t 轮开始前的历史、过去反馈、随机数和参数版本。
条件于这些已有信息，本轮参数 $\phi_t$ 已经确定。再固定本轮噪声，就固定了提议概率表 $q_t$。
式 (16)–(19) 对任何这样的 q 都成立。

先用本轮原 q 完成接受／拒绝，再计算 $\phi_{t+1}$；新参数从下一轮开始生效。
对各种可能的过去取平均（全概率公式），本轮输出服从基座条件分布；逐轮归纳得到完整序列结论。
校正负责保持目标分布；学习通过改变 q 影响接受长度和运行成本。

实现按这一顺序同步执行：固定本轮 q，完成验证，提交合法路径的基座 KV，更新适配器，开始下一轮。
基座权重在整个请求流中保持固定。

## 11. 前缀树的构建、验证与在线反馈

线性方案每个位置抽一项。低 batch 时可以增加候选，把前缀按 $\sum_i\log q_i(y_i)$ 排序，
保留有限个前缀，用树注意力一起验证。每个节点读取共同历史、祖先与自身，位置编号按深度设置。

树由 top-k 候选确定性构建，生成时在已验证节点直接从目标 p 抽下一项：
存在对应子节点就继续，走出树时发出该 token 并结束本轮。
对于任何固定树，每一次抽样都服从目标条件分布；树决定一次能复用多少已算好的 logits。
该遍历适用于贪心和随机目标。原文第 4.1.2 节说明 top-k 与前缀预算，附录 B.3 的完整伪代码是线性路径；
固定公开版本的 [`tree_verify_stage`](https://github.com/ifm-ai/uno/blob/ed2ee36bb7a3aea8732ebc635b3f09490a032ea3/nano_vllm_uno/engine/two_pass_decoding.py)
同样先从目标 logits 抽 token，再调用树遍历。

### 11.1 前缀树构建与证明

[tree.py](../src/blockspec/tree.py) 用一个最大优先队列按前缀分数取节点。根计入预算 V，
每次取出最高分可扩展前缀，再把它的至多 k 个孩子加入队列。每项概率都在 [0,1] 中，
所以孩子的概率乘积小于或等于父亲；每个深节点之前都存在分数更高或相等的祖先。
因此优先队列可逐个取出每位置 top-k 范围内的全局最高分前缀，搜索量由预算 V 控制。
测试枚举小树的全部前缀并排序，与队列输出逐一对比。

正温度时，树分数使用按该温度缩放的学生软概率；贪心目标使用温度 1 的软分数构建多分支。
树按学生的全词表软分布评分，再应用候选 top-k 选择节点；目标抽样使用自己的温度与截断设置。

### 11.2 目标路径遍历、缓存压缩与在线反馈

设树节点 n 对应路径 $y_0,\ldots,y_d$。通过树 mask 和按深度的位置编号，其输出分布等于
单独运行该路径的 $p_\theta(\cdot\mid h,y_0,\ldots,y_d)$，这有每个节点的数值对照。
从该分布直接抽下一项：沿对应孩子继续遍历，走出树时发出该 token 并结束本轮；到达叶子后再抽一项作为额外输出。
对固定树反复使用条件概率乘法规则，即证明最终输出服从 AR。走出树的 token 也直接来自 p，完成该轮输出。

验证后收集提交路径的节点索引，把干净历史和这些祖先节点的 KV 按路径顺序 gather；
最后一个输出作为下一轮种子，其 KV 在下一轮计算。
测试逐节点比较压缩缓存和独立路径缓存，并覆盖根节点预算、分支、短尾和 EOS。

在线反馈采用实际遍历节点的老师 logits，按深度匹配原噪声输入的第 1、2、…行。
走出树的位置同样提供有效老师目标，最多与 B−1 个学生位置配对。
同一个 `OnlineLearner` 在树解码后使用这些反馈续训适配器。
树统计中的 `proposed` 是根之外的候选节点总数，`accepted` 是实际匹配的子节点数；
每轮输出长度反映树对目标路径的覆盖程度。

固定已提交历史 h、已抽出的根 $y_0$ 和当前树后，可以精确计算平均每轮输出。
令 $\rho_n$ 表示目标生成路径到达节点 n 的概率，根的概率为 1。对父亲 a 和孩子 n，有

```math
\rho_0=1,\qquad
\rho_n=\rho_a\,p_\theta(y_n\mid h,\operatorname{path}(a)).
```

这里 `path(a)` 包含从根到 a 的所有 token。到达一个节点后，再读取该节点的目标概率表并发出下一项。
因此在输出预算充足、EOS 关闭时，输出数等于“根的 1 项，加上每个到达节点发出的 1 项”：

```math
\mathbb E[N\mid h,y_0,\mathcal T]
=1+\sum_{n\in\mathcal T}\rho_n
=2+\sum_{n\in\mathcal T\setminus\{0\}}\rho_n.
```

证明只需要给每个节点设置一个“本轮到达过”的指示变量，再对它们求和、取期望。
同一深度所有节点的到达事件互斥，各深度之和就是该深度的路径覆盖概率。
例如树中仅有根与一个到达概率为 0.7 的孩子时，平均输出为 $2+0.7=2.7$ 项。

输出预算为 M、EOS 开启时，仅统计深度 $d_n\le M-2$ 且 token 为普通项的节点；
孩子的到达概率递推还乘上父亲为普通项的指示变量。此时仍有
$\mathbb E[N]=1+\sum_n\mathbf1[d_n\le M-2,\ y_n\ne EOS]\rho_n$。
测试穷举目标抽样的终止路径，覆盖短预算、根 EOS 和内部 EOS，验证该期望与实际遍历相等。

这也给出了增大块长与树预算的评价依据：新增节点贡献其目标到达概率，同时增加验证成本。
学生的概率乘积用于预测这些到达概率；在线软分布监督用实际到达历史修正学生。
块长、预算和更新频率的选择共同使用第 12 节的净吞吐条件。

### 11.3 独立实现与固定公开实现的契约对照

`scripts/audit_sampler_reference.py` 从固定 Git 对象读取 CPU 树构建与遍历定义，进行输入／输出契约对照。
16 个前缀分数互异的树构建配置、1,600 组预先抽定的目标 token 路径，节点、父亲、深度、提交序列及缓存路径均匹配。
结果连同 commit 和源文件 SHA 输出到 stdout。

同一脚本还读取固定版本 `training/losses.py` 的全词表 L1 与反向 KL 定义，
在 7、103、2,111 项词表上检查损失值、学生梯度和老师梯度隔离。
源码核对覆盖 `training/trainer.py` 的监督位置、`training/data.py` 的提示／回答划分，
以及 `two_pass_decoding.py` 的种子位置、两次前向、树路径提交和 KV 边界。
公开 Qwen 配方使用回答区域监督和较长上下文；当前小规模训练采用独立样本的连续裁剪，训练配置与来源随检查点保存。

本项目同分数时按 token 路径打破平局，参照实现使用深度、候选排名等次级键。
小词表测试还穷举了两步目标联合分布，覆盖沿树继续、走出树再恢复 AR 的情况。

## 12. 在线训练的计算成本

单项 token 通过一个 $d_{in}\to d_{out}$ 线性层，基座前向约有 $2d_{in}d_{out}$ 次浮点运算，
低秩附加前向约有 $2r(d_{in}+d_{out})$ 次。
为更新前面层的适配器，梯度还要穿过后面冻结层，计算输入梯度 $W^\top g$。

在线成本还包括学生重放前向、激活存储、词表投影、概率损失、Adam 状态读写、反馈复制和同步。
`update_seconds` 记录更新区域，净 TPS 由整条生成流程的输出数与耗时计算。

### 12.1 从接受长度到净收益条件

在线性路径中，令 K 为一轮接受的噪声候选数。完整非 EOS 轮产生 $N=2+K$ 个 token，用两遍前向；全接受时 N=B+1。
非负整数的尾和公式给出：

```math
\mathbb E[N]=2+\sum_{i=1}^{B-1}\Pr(K\ge i).                           \tag{21}
```

证明：每个整数 k 都等于 $\sum_i\mathbf1[k\ge i]$，两边取平均即可。
单位置接受率是 $1-D_{TV}(p,q)$。连续 i 项的接受概率由每一步在前面均已接受条件下的接受率相乘得到。
若这些条件接受率都近似为同一个 a，便有 $\Pr(K\ge i)\approx a^i$；实际测量直接统计每轮的 K。

设 AR 每项平均耗时 $C_A$；每轮起草、验证、调度分别为 $C_D,C_V,C_H$；一次更新为 $C_U$，
平均 S 轮更新一次。忽略短尾、假设稳定工作负载：

```math
TPS_{static}\approx\frac{\mathbb E[N]}{C_D+C_V+C_H},\qquad
TPS_{online}\approx\frac{\mathbb E[N_{on}]}{C_D+C_V+C_H+C_U/S}.         \tag{22}
```

在线超过 AR 需要：

```math
\mathbb E[N_{on}]C_A>C_D+C_V+C_H+C_U/S.                               \tag{23}
```

在起草、验证和调度成本相同的条件下，在线版超过静态版还需：

```math
\frac{\mathbb E[N_{on}]}{\mathbb E[N_{static}]}
>1+\frac{C_U/S}{C_D+C_V+C_H}.                                        \tag{24}
```

式 (24) 给出了学习成本所需的接受长度增幅。测量时将 prefill、EOS 短尾和学习过程一并计入，
预热后交换方法的执行顺序，并重复运行以观察 GPU 负载引起的波动。

### 12.2 末层特征重放节省了哪一部分

全量重放的 $C_U$ 包括所有层的学生前向与必要输入反向。9.3 的方法重算后 k 层，为这些层的适配器维护梯度与 Adam。
它的计算包含后段前向／反向、监督位置的全词表投影和损失。起草、验证与 AR 基线使用同一个完整基座。

如果新增特征保存和检查的平均每轮成本为 $C_F$，式 (22) 的在线分母相应成为
$C_D+C_V+C_H+C_F+C_{U,k}/S$。k 同时影响更新耗时和可学习的参数范围，选择时比较净 TPS 与接受长度。
28 层 r=32 示例中，全适配器为 27,983,872 项，最后 4 层为 3,997,696 项，即全量参数的 1/7。
更新耗时由这些参数对应的计算量、词表投影和状态读写共同决定。
B=4、隐藏维度 1536、FP32 时，一份 H 为 $4\times1536\times4=24,576$ 字节；
完整反馈还包含后段 KV、位置、mask 和老师词表 logits。

### 12.3 图准备与流级吞吐

把每次请求的 prefill、生成、反馈复制和学习合计为请求流耗时 T，输出数为 N，
一次性推理图与训练图准备合计为 F、learner 初始化为 I，可以分别计算：

```math
TPS_{请求流}=\frac{N}{T},\qquad TPS_{含准备}=\frac{N}{T+F+I}.
```

含准备的加速比使用两种方法各自的 $N/(T+F+I)$ 相除。
三路评测分别按各方法单独部署所需的图形状计入 F，其中包括 AR 使用的短 prefill 形状。
若两种方法的每项成本近似为 $c_A,c_S$，流级净加速的条件为：

```math
N(c_A-c_S)>F_S+I_S-F_A-I_A.
```

左边是 N 项输出累计节省的时间，右边是额外准备时间；请求流越长，准备费用分摊到每项后的数值越小。
固定容量图还会处理被 mask 的注意力位置，并占用相应存储。容量选择因此同时考虑历史长度、执行时间和显存。

### 12.4 沿请求顺序计算学习收益

第 j 个请求输出 $n_j$ 项、耗时 $t_j$、执行 $r_j$ 个起草轮次。
累计量为 $N_j=\sum_{i\le j}n_i$、$T_j=\sum_{i\le j}t_i$、$R_j=\sum_{i\le j}r_i$，
因此截至该请求的吞吐和平均每轮输出分别为 $N_j/T_j$、$N_j/R_j$。
含准备吞吐按 $N_j/(T_j+F+I)$ 计算；图跨重复轮次复用，其费用归入第一轮，learner 初始化按各次请求流计入。

三路输入按相同请求顺序排列。固定预算下，第 j 个请求的在线时间收益为
$t_{static,j}-t_{online,j}$，正值表示在线版节省了时间；轮次收益为 $r_{static,j}-r_{online,j}$。
将它们沿请求流相加，就能观察接受长度改善何时覆盖累计学习成本。
`benchmark.py` 的 `trajectories` 返回逐请求与累计指标，并记录请求开始／结束时的适配器版本和最近一次更新损失。
`feedback_blocks` 统计实际保存的反馈块，便于把反馈采集量、更新次数和耗时对应起来。

对输出预算相同的两个请求流，还可以直接计算截至第 j 个请求节省的秒数。
令 $T_{static,j},T_{online,j}$ 为累计请求耗时，$F_{static},F_{online}$ 为各自图准备成本，$I_{online}$ 为 learner 初始化成本：

```math
\Delta_j=T_{static,j}-T_{online,j},\qquad
\Delta_j^{setup}=\Delta_j-(F_{online}+I_{online}-F_{static}).        \tag{24a}
```

正值表示在线版累计节省时间，负值表示在线版累计多用时间。两边输出数相同，吞吐大小由分母的时间决定，
所以 $\Delta_j^{setup}>0$ 就等价于在线版在计入准备后取得更高吞吐。
每个时点同时报告减少的生成轮次和累计学习耗时，便于把接受长度变化与实际时间成本联系起来。

### 12.5 覆盖触发的成本

令 $\gamma$ 为检查时点中 G=0 的比例，一次门控检查耗时为 $C_G$，一次实际训练平均耗时为 $C_U$。
在稳定请求流的近似下，每轮学习与门控成本为：

```math
C_{learn}\approx\frac{(1-\gamma)C_U+C_G}{S},\qquad
TPS_{coverage}\approx\frac{\mathbb E[N_{coverage}]}
{C_D+C_V+C_H+C_F+C_{learn}}.                                      \tag{25}
```

与周期更新相比，门控直接节省约 $\gamma C_U/S$ 的每轮训练时间，同时产生门控检查成本，
并通过新的参数轨迹改变平均每轮输出。比较净 TPS 时把这三项一并计算。
实际实现还包含请求边界和短尾，`updates` 报告实际训练次数，`coverage_skips` 报告覆盖完整而保留状态的检查次数，
`fully_covered_rounds` 报告实际完整覆盖的验证轮次。门控检查纳入生成总时间，`update_seconds` 记录执行训练的区域。

### 12.6 温度采样与评测口径

生成温度、目标 top-k 和 top-p 共同确定目标分布。先将 logits 除以正温度 τ，保留概率最高的 K 项，
再按降序选择累计归一化概率首次达到 ρ 的最短前缀。K=0 表示使用完整词表，ρ=1 表示保留全部候选。
令最终保留集合为 S，则目标概率为：

```math
p_{\tau,K,\rho}(v\mid h)
=\frac{\mathbf 1[v\in S]\exp(l_v(h)/\tau)}
{\sum_{u\in S}\exp(l_u(h)/\tau)}.
```

AR、静态和在线三路使用同一组 τ、K、ρ，预热与正式请求也使用相同配置。τ=0 进入贪心专用路径。
线性起草使用相同设置处理 q，接受校正使用实际的 p 与 q；因此截断后的零概率位置也能按第 7 节公式处理。
树构建先用温度缩放后的完整学生分布评分，以候选宽度和前缀预算选择树节点；沿树抽样使用目标 p。
命令行的 `--top-k` 表示树候选宽度，`--sampling-top-k` 表示目标概率筛选，两个参数分别记录。

随机数种子固定每条路径自身的实验结果。AR 每次抽样产生下一项；线性方法还抽噪声、草稿和接受随机数，
树方法也抽噪声，因此相同种子在三条路径上对应各自的随机数消耗序列。
贪心验收逐项比较 token；随机采样验收检查输出概率规律，同时把实际 token 比较保留为观测信息。
结果分别标记为 `greedy_exact` 与 `stochastic_diagnostic`，后者的 `greedy_identical` 为 null。
概率测试穷举候选、接受、拒绝及校正分支，覆盖温度和截断组合，核对最终概率等于 p、接受概率等于 1−TV(p,q)。

两路与三路入口均报告总输出项数除以总耗时，并提供平均每轮输出和平均每次解码前向输出。
后一个指标为 `tokens_per_decode_forward`；prefill 的时间归入请求耗时，前向计数统计生成阶段。
在线蒸馏继续比较温度 1 的完整词表软分布，训练 TV 在其他生成设置下作为接受质量的代理指标。

## 13. 当前实现和验证证据

模型、训练和推理主体位于 `src/blockspec`。加载器读取本地配置与 safetensors，核对模型家族、
投影维度、位置编码和权重形状，再映射到本项目的 `Decoder`。当前完整基座对照使用 K2-Horizon-0.9B，
包含 1,078,285,824 个基座参数。

| 内容 | 实现 | 验证 |
| --- | --- | --- |
| 因果模型、条件低秩层、KV | `model.py` | 显式注意力、缓存／全量、多种 head_dim、数值微分 |
| 分组短查询注意力 | `attention.py` | 按头对照、Q/K/V 梯度、有限差分、树 mask、跨请求参数与 Adam |
| 固定形状执行 | `execution.py` | CUDA 图重放、padding／树等价、快照独立存储、参数原地更新可见 |
| 末层梯度图 | `replay_execution.py` | 固定容量梯度、重复签名累积、连续 Adam 更新、准备成本 |
| 噪声与逆向核 | `diffusion.py` | 小词表穷举后验、一步退化 |
| 配对蒸馏、课程 | `distillation.py`、`training.py` | 老师隔离、分块等价、基座权重固定、损失日程 |
| 验证解码 | `sampling.py`、`decoding.py` | 支持集分离、拒绝采样、全接受、EOS、短尾 |
| 目标路径树 | `tree.py` | 最佳前缀穷举、每个节点独立路径 logits／KV、EOS、在线参数更新 |
| 在线续训 | `online.py` | 全量／末层子集更新、有效位置、跨请求版本、特征与选行投影梯度、反馈窗口、覆盖门控与优化器状态对照 |
| 检查点与数据 | `checkpoint.py`、`data.py` | 权重存取、基座匹配校验、新文件写入、独立样本 |
| 全流程 | `python -m blockspec demo` | 小基座→适配器→保存加载→静态／在线输出一致 |
| 完整成本 | `benchmark.py` | 顺序交换、累计计时、跨请求续训、实验后还原适配器 |

在 WSL2 / PyTorch 2.11.0+cu128 上 **432 项测试通过**，覆盖 CUDA 图、小型 Qwen3 数值参照、树路径、
固定验证、按问题分组的数据划分，以及低精度基座上 FP32 适配器主权重的精确存取。
小型周期序列测试检查损失下降、参数更新和输出一致性；自然语言请求流的吞吐测量见 13.3。

外部数值参照脚本 `audit_hf_reference.py` 先核对固定版本的代码、配置、权重及索引，再加载基座作者的完整模型，
逐项确认两边基座张量一致。参照依赖 Transformers 5.15.0 隔离在仓库外，训练环境使用 4.55.0；
该脚本为本项目实现提供外部测试参照。来源与 hash 见 [固定来源清单](../references/upstream.lock.json)，
模型作者的接口说明见 [模型页](https://huggingface.co/IFM/K2-Horizon-0.9B#transformers)。

FP32、4 个验证记录各 512-token 前缀，再沿参照轨迹做 64 步缓存解码；还检查位置偏移 8,192 和 65,536 的短窗口。
分组短查询图版本的 2,048 个 prefill 位置最大 logits 绝对差约 $1.29\times10^{-4}$；256 个图缓存解码位置约 $7.15\times10^{-5}$；
256 个移位位置约 $9.51\times10^{-5}$。三组最大 TV 分别约为 $2.16\times10^{-5}$、$2.43\times10^{-6}$、$8.80\times10^{-6}$，
贪心项均相同，通过预先给定的 logits $5\times10^{-4}$、TV $10^{-4}$ 及零 argmax 差异门槛。
其中 prefill 使用普通路径，逐 token 与 32 项移位窗口使用图路径。
普通执行器另以 128-token 前缀和 32 步缓存解码通过外部对照。
移位窗口专门检查大位置编号下的旋转编码计算。

### 13.1 低精度差异的逐层定位

`audit_model_precision.py` 在同一基座上比较普通／配对前向，将逐层差异输出到终端，并在完成后恢复 hook 和全局精度开关。
32 个随机词表 token 的受控探针中，第一处差异已经出现在首层 Q 投影，早于注意力；
输入 RMSNorm 则相同。关闭 BF16 的低精度中间归约后，首层最大差异由 0.0625 降为 0.00390625，
后续层继续传播和放大这部分差异。
这种批运算与切片运算的舍入差异符合 [PyTorch 2.11 的数值说明](https://docs.pytorch.org/docs/2.11/notes/numerical_accuracy.html)。

逐步误差还可以通过耦合推导到整条序列：若第 t 步在每个共同历史上的目标核 TV 误差至多为 $\epsilon_t$，
最大耦合使该步输出相同的概率至少为 $1-\epsilon_t$。连续各步均相同的概率至少为这些数值的乘积，
于是序列分布的 TV 距离至多为 $1-\prod_t(1-\epsilon_t)\le\sum_t\epsilon_t$。
该式将每步目标分布的误差上界与整条序列的误差联系起来。当前吞吐基准采用 FP32。

贪心选择还取决于前两名的间隔。设原 logits 最大项与次大项相差 $\delta$，每个 logits 的绝对误差至多为
$\epsilon$。更新后的原最大项与其他任意项之间，间隔至少为 $\delta-2\epsilon$；因此
$\delta>2\epsilon$ 足以保证最大项相同。这个条件把“数值接近”和“选择相同”联系起来。

13.4 的公开适配器树对照在第 17 条提示的第 126 个生成 token 出现一个具体例子。
`audit_decode_path.py` 沿共同历史对齐实际到达的祖先路径：AR 的两项 logits 为 29.17294693、29.17294312，
间隔 $3.81\times10^{-6}$；树验证的这两项同为 29.17294884。
整行最大绝对差为 $1.72\times10^{-5}$，概率 TV 为 $3.12\times10^{-6}$。
AR 选中 token 56534，树路径的并列选择取 token 2715；此前 125 个生成 token 相同。
这次审计记录了实际目标行与数值选择，后续数值验收同时检查概率误差和完整输出。

### 13.2 适配器的离线训练

真实基座上从随机 r=32 全适配器开始，1,200 步、每步 2×256 token，总计 614,400 个训练 token；
前 300 步使用反向 KL＋L1，之后纯 L1，块长按 2→4→6→8 推进、各占 300 步。
学习率 $3\times10^{-4}$，$\alpha=32$、缩放 s=1，全程 FP32，种子 314159。
适配器有 27,983,872 个参数，训练连同固定验证耗时 522.41 秒，训练前后基座指纹相同。
联合热身目标的公式与梯度见 5.2。

| 块长 | TV 距离：训练前→后（越小越好） | 最大概率项一致率：训练前→后 |
| --- | --- | --- |
| B=2 | 0.9280 → 0.5936 | 5.54% → 37.25% |
| B=4 | 0.9409 → 0.6844 | 3.82% → 26.76% |
| B=6 | 0.9480 → 0.7308 | 2.65% → 21.27% |
| B=8 | 0.9500 → 0.7615 | 2.40% → 18.73% |

每个块长使用同一组 2,040 个有效位置，窗口和噪声固定；训练前、每 300 步及结束时验证。
训练 72 条、验证 17 条按问题分开，另有 23 条保留测试。验证以固定权重前向计算。
表中指标描述给定数据前缀时的老师／学生概率差异；自由生成时的接受长度与 TPS 在下一节测量。
训练后的四种块长均表现为 TV 降低、最大概率项一致率提高。
当前检查点及数据来源清单存放在仓库外，配置、数据 SHA、实现指纹随检查点保存。

随后从该起点继续训练固定 B=4 的适配器：训练集扩展到 619 条，窗口改为 512 项，
每步 2 个窗口、1,200 步，共增加 1,228,800 个训练 token；L1、学习率 $10^{-4}$，r=32、α=32 保持原值。
续训使用新 Adam，训练连同验证耗时 1,036.26 秒，基座保持固定。
同一组 4,088 个固定验证位置上，TV 从 0.68815 降至 0.66591，最大概率项一致率从 27.47% 升至 29.72%。
窗口与噪声指纹为 `04f63324a55db71d409c8e2854303344a79c9425cca140aef4c953b73f95e5c4`。
两次训练的验证窗口分别为 256 和 512 项，各自使用本轮训练前后的成对指标。
起始检查点 SHA 为 `66181b0055e2731e5ea728af56f15fca7d6786821ebf12ba99ee7edf4ba04462`，
续训检查点 SHA 为 `43cadafa30a00d5a9683b405082402057794d49bde182ef23f92930acaf85406`，
扩展训练文件 SHA 为 `c49addb18df1eeea7a8aaddb115968fac03ff79bcd8db7bb681fdc7f9146fc96`。

### 13.3 小模型离线收益的配对测量

当前优先加强固定离线适配器。独立入口 `scripts/benchmark_offline.py` 在每个请求交换 AR／静态的先后顺序，
下一轮重复再次反转顺序；两路共享精度、注意力算法、提示、输出预算和 GPU 图实现。计时从 prefill 前开始。
每个方法分别计入其单独部署时所需的执行图准备成本。所有参数在整个测量中固定。

2026-09-06 的当前两路完整测量采用 13.2 的固定 B=4 续训检查点，FP32、全部 17 个验证请求，
256 项前缀、512 项输出、32 项预热、两轮重复，每种方法共 17,408 项输出。
树配置为 B=4、K=8、V=16，使用分组短查询、低秩空间门控和共享历史输入工作区。

| 方法 | 总生成秒数 | TPS | 含准备 TPS | 平均每轮输出 |
| --- | --- | --- | --- | --- |
| 普通自回归 | 164.988 | 105.51 | 105.20 | 1.000 |
| 固定离线适配器 | 153.816 | 113.17 | 111.71 | 3.753 |

生成吞吐比为 **1.073×**，含准备为 **1.062×**。
静态执行 4,638 轮、9,266 次解码前向，34 组输出全部逐 token 一致，基座和适配器指纹保持固定。
该检查点作为后续在线续训的离线起点。小模型的收益验收同时使用配对吞吐、逐 token 比较和完整准备成本。

离线检查点 SHA 为 `43cadafa30a00d5a9683b405082402057794d49bde182ef23f92930acaf85406`，
验证文件 SHA 为 `27de07376f8ff0c100782da6e5e69defac9ae5ce353a806209da16e3a8f72eb2`，
本场起始实现指纹为 `b3dbf15cfe9bb16a3e64f70efa8a192074a15dabec88ab2e4d96e770f063c419`。

### 13.4 公开适配器对照

原仓库提供与当前基座对应的 [1B 适配器](https://huggingface.co/IFM/K2-Horizon-0.9B-Uno)。
`adapter_io.py` 读取 PEFT 配置，核对 rank、缩放、七种目标投影、每个 A/B 张量的形状及完整文件 SHA，
然后把权重映射到自有模型；全部校验完成后统一发布参数。评测输出将此来源标为 `published_peft_reference`，
本项目训练的检查点标为 `local_training`。两类权重在同一个独立解码器中测量。

公开 1B 适配器 r=128、$\alpha=2048$，111,935,488 个参数；392 个张量映射到当前基座。
固定来源 revision 为 `b0d8896a301a2f4bc755538b1234a35100da50d0`，
权重 SHA 为 `5a499229d19ef4a69eb0b21884819d1b67cd983ba02b7ee2031ba8567dedfe4e`。
它用于区分已训练起草器的能力与本地执行成本。

同日以公开适配器完成 17 个请求、两轮重复的独立解码器对照。精度、提示、输出长度和树配置沿用 13.3，
执行器使用共享历史输入工作区。每路生成 17,408 个 token，结果如下：

| 方法 | 总生成秒数 | TPS | 含准备 TPS | 平均每轮输出 |
| --- | --- | --- | --- | --- |
| 普通自回归 | 165.661 | 105.08 | 104.94 | 1.000 |
| 公开适配器，B=4 / K=8 / V=16 | 152.624 | 114.06 | 112.66 | 3.781 |

生成吞吐比为 **1.085×**，含准备为 **1.074×**，两轮分别为 1.088×、1.083×。
适配器执行 4,604 轮、9,203 次解码前向；基座和适配器保持固定，峰值分配显存 5.157 GiB。
34 组比较中 32 组逐 token 一致，第 17 条提示在两轮的第 126 个生成 token 分叉。
本场实现指纹为 `b3dbf15cfe9bb16a3e64f70efa8a192074a15dabec88ab2e4d96e770f063c419`。

计时关系可以直接用第 12 节公式核对：普通 AR 平均每 token 9.516 ms；起草与验证合计每轮平均 33.150 ms，
这两个平均值含各自请求开销的摊销。于是 $3.781\times9.516/33.150\approx1.085$。
本场平均每次解码前向产出 1.892 个 token；前向的查询宽度和适配器计算会影响每次调用的耗时。
公开模型页的 TPF 也是按前向次数计量的指标，实际吞吐同时取决于这些执行成本。

补充对照沿用公开入口的 B=8 线性块形状，保持上述本机精度与请求条件：AR 为 105.17 TPS，
起草验证为 99.70 TPS，吞吐比 0.948×，含准备 0.940×；每轮产出 3.361 个 token，34 组输出全部一致。
该负载中较长线性块的接受长度与执行成本共同拉低吞吐，后续配置选择以短块树为起点。

在线接入沿用第 9 节实现。上一阶段 SDPA 执行后端中，静态与在线为 89.81、89.10 TPS，
在线平均每轮输出增加 0.262%，231 次更新耗时 2.394 秒。
这组结果用于解释在线收益与成本；当前阶段以加强离线基线为先。

## 14. 实验设计与持续更新

当前离线训练数据来自 [OpenThoughts3-1.2M 官方数据集](https://huggingface.co/datasets/open-thoughts/OpenThoughts3-1.2M)
的子集。该数据集同一问题有多份回答，因此按规范化问题 hash 分组，使同一问题的回答归入同一划分。
当前 112 条记录含 111 个问题，分为训练 72、验证 17、保留测试 23；含代码 24、数学 80、科学 8 条。
训练集用于适配器学习，验证集用于配置选择与工程评测，保留测试用于设计确定后的最终评测。

短块训练另准备同一组页面起点、每页 64 条的扩展数据：896 条记录、882 个问题，划分为训练 619、验证 147、测试 130。
领域为代码 192、数学 640、科学 64。扩展集沿用相同的问题分组和种子，与原验证／测试集合按问题保持划分一致。
训练入口继续核对实际使用的训练文件与验证文件，吞吐对照沿用原 17 个请求。

准备入口读取预定页面，按基座本地聊天模板编码，保存 token、来源行号、问题／文本 hash 和各文件 hash；
采集时选择完整单元格，并记录观察到的仓库 revision 与实际响应 hash，形成可核验的来源清单。
数据和 manifest 位于仓库外，数据准备实现保存在仓库内。

后续实验围绕四个问题展开：

1. 历史长度：扩大完整基座与外部参照的长历史数值对照，测量图容量对时间和显存的影响。
2. 离线学习：扩大训练问题覆盖，比较缩放系数与学习率，联合观察固定前缀 TV、自由生成接受长度及净 TPS。
3. 推理执行：优化 KV 读写与投影调用，在 AR、静态和在线三路中共享适用的执行优化。
4. 在线适应：沿更长的主题请求流评估覆盖触发与末层梯度图，联合选择更新频率、重放长度和可训练层数，核算准备成本的摊销。

所有方法固定精度、采样设置、提示、预算和 KV 条件，预热后正反顺序成对运行。
随机采样比较统计分布，贪心生成逐项比较输出。配置通过验证集选择，最终在保留测试上评测。
主报告随设计和实验同步更新，集中保留当前原理、配置、数据、结论和运行入口；版本演变由 Git 记录。
